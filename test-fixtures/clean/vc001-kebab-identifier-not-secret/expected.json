{
  "description": "VC001 must not fire on SCREAMING_CASE _KEY assignments whose value is a lowercase slot identifier — kebab-case OR snake_case (localStorage keys, cookie names, cache keys). These are NOT credentials.",
  "expectedFindings": [],
  "mustNotFire": ["VC001"]
}
