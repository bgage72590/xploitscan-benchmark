{
  "description": "Sequelize .find({ where }) scoped by ownerId. The lookup-shape widening must not turn correctly-scoped Sequelize queries into findings.",
  "expectedFindings": [],
  "mustNotFire": ["VC158"]
}
